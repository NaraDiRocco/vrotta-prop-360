{
  "name": "@r360/rls-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Harness de RLS (Postgres) contra Supabase local. NO es un paquete del workspace (no matchea packages/* ni apps/* en pnpm-workspace.yaml) a propósito: así 'pnpm -r test' en la raíz nunca lo toca. Se corre aparte, ver README.md.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "vitest": "^3.2.7"
  }
}
